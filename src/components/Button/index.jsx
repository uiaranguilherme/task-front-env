import { Container} from './styles';

const Button = () => {
    
    return(
        <Container color={"black"}>
            <button>Create</button>
        </Container>
    );
}
export default Button